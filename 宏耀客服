以下是 **宏耀客服项目** 各模块的详细总结与功能说明，基于项目文件树和配置文件内容整理：

---

### 一、核心业务模块

#### 1. **微信管理模块**
- **功能概述**：实现多微信账号的管理、消息解析和智能回复。
- **核心文件**：
  - `message_parser.py`：解析微信消息（文本、图片、文件等）。
  - `auto_replier.py`：集成NLP模型，实现智能回复。
  - `multi_instance.py`：支持多开控制，实现进程隔离和资源分配。
- **配置文件**：
  - `wechat_api.yaml`：配置微信账号信息、回调地址和接口调用频率限制。

#### 2. **数据输入模块**
- **功能概述**：支持Excel文件的数据导入和校验。
- **核心文件**：
  - `excel_processor.py`：解析Excel文件（xls/xlsx/csv格式）。
  - `data_validator.py`：校验数据格式和业务规则。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 3. **风控引擎模块**
- **功能概述**：实现合规检查、数据脱敏和审计日志记录。
- **核心文件**：
  - `compliance_checker.py`：动态加载合规规则，检查数据合法性。
  - `data_anonymizer.py`：实现k-匿名和差分隐私脱敏。
  - `audit_logger.py`：记录审计日志，支持区块链存证。
- **配置文件**：
  - `compliance_rules.json`：定义GDPR和网络安全法的合规规则。

---

### 二、AI处理层

#### 1. **联邦学习模块**
- **功能概述**：实现分布式模型训练和安全聚合。
- **核心文件**：
  - `coordinator.py`：协调节点，管理任务分发和结果聚合。
  - `secure_aggregation.py`：实现Paillier同态加密的安全聚合。
- **配置文件**：
  - `federated_learning.yaml`：配置聚合周期、节点权重和隐私保护参数。

#### 2. **传统机器学习模块**
- **功能概述**：实现工单分类和满意度预测。
- **核心文件**：
  - `classifier.py`：基于TF-IDF和LightGBM的工单分类模型。
  - `regression.py`：基于时间序列分析的满意度预测模型。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 3. **大模型集成模块**
- **功能概述**：集成DeepSeek大模型，实现智能对话和提示工程优化。
- **核心文件**：
  - `deepseek_api.py`：封装大模型API，支持自动退避重试。
  - `prompt_engineer.py`：优化提示词工程，提升模型响应质量。
- **配置文件**：
  - `deepseek_api.key`：存储加密后的大模型API密钥。

---

### 三、交互接口层

#### 1. **人机界面模块**
- **功能概述**：提供Web前端和RESTful API接口。
- **核心文件**：
  - `frontend/`：基于Vue3+TS的前端工程。
  - `backend_api.py`：实现JWT鉴权和RESTful接口。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 2. **地图服务模块**
- **功能概述**：集成高德、百度、腾讯地图API，实现POI搜索和坐标转换。
- **核心文件**：
  - `amap_adapter.py`：高德地图接口封装。
  - `baidu_map.py`：百度地图接口封装。
  - `coordinate_utils.py`：实现WGS84/GCJ02坐标转换。
- **配置文件**：
  - `map_credentials.json`：存储地图API密钥和服务地址。

#### 3. **开发者接口模块**
- **功能概述**：提供Python SDK和Jupyter扩展，支持开发者自定义功能。
- **核心文件**：
  - `sdk/`：Python SDK，封装核心业务接口。
  - `jupyter_ext/`：Jupyter扩展，支持可视化调试。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

---

### 四、服务模块

#### 1. **爬虫系统模块**
- **功能概述**：实现招聘网站数据爬取和反反爬策略。
- **核心文件**：
  - `base_crawler.py`：爬虫基类，实现请求重试和异常处理。
  - `zhaopin_spider.py`：智联招聘爬虫。
  - `58tongcheng.py`：58同城爬虫。
  - `qiancheng.py`：前程无忧爬虫。
  - `anti_anti_crawler/`：反反爬子系统（IP代理池、验证码识别、指纹伪造）。
  - `data_quality/`：数据清洗模块（结构校验、去重）。
- **配置文件**：
  - `crawling_policies.yaml`：配置爬虫的请求频率限制和黑名单。

#### 2. **部署支持模块**
- **功能概述**：实现环境检测、一键安装和桌面启动器。
- **核心文件**：
  - `dependency_check.py`：检测Python和CUDA环境。
  - `auto_install.sh`：一键安装脚本。
  - `shortcut_win.ps1`：生成Windows快捷方式。
  - `shortcut_linux.sh`：生成Linux桌面启动器。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 3. **风险监控模块**
- **功能概述**：实现实时告警和用户行为分析。
- **核心文件**：
  - `realtime_alert.py`：分级告警系统（邮件、短信、电话）。
  - `behavior_analyzer.py`：基于时序模式的行为分析。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

---

### 五、工具与文档

#### 1. **通用工具模块**
- **功能概述**：提供日志记录、配置加载和变量管理功能。
- **核心文件**：
  - `logger.py`：异步日志记录，支持写入Elasticsearch。
  - `config_loader.py`：动态加载配置文件，支持环境变量覆盖。
  - `variable_manager.py`：统一管理全局变量。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 2. **文档模块**
- **功能概述**：提供安装指南、API文档和合规手册。
- **核心文件**：
  - `install_guide/`：Windows和Linux安装指南。
  - `api_reference/`：OpenAPI规范文档和SDK使用说明。
  - `risk_management/`：合规操作流程和应急响应预案。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

---

### 六、数据与测试

#### 1. **数据存储模块**
- **功能概述**：存储训练数据、模型快照和运行时缓存。
- **核心文件**：
  - `training_data/`：标注数据集和模型检查点。
  - `runtime_cache/`：微信会话记录和爬虫临时数据。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 2. **测试模块**
- **功能概述**：实现单元测试和集成测试。
- **核心文件**：
  - `unit_tests/`：单元测试用例（消息解析、数据导入）。
  - `integration_tests/`：集成测试用例（爬虫系统、风控引擎）。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

---

### 七、运维脚本

#### 1. **环境初始化脚本**
- **功能概述**：构建虚拟环境和依赖安装。
- **核心文件**：
  - `setup_env.py`：环境初始化脚本。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 2. **服务监控脚本**
- **功能概述**：监控服务进程状态。
- **核心文件**：
  - `service_monitor.sh`：进程存活检测脚本。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

#### 3. **代理池管理脚本**
- **功能概述**：维护代理IP池的可用性。
- **核心文件**：
  - `proxy_pool_manager.py`：代理IP池管理工具。
- **配置文件**：
  - 无独立配置文件，依赖全局变量管理模块。

---

### 总结

    通过以上模块的划分和功能说明，**宏耀客服项目**实现了从微信管理、数据爬取到AI处理的完整闭环。各模块之间通过清晰的接口契约和配置文件进行交互，确保系统的高内聚、低耦合和可扩展性。
